Employee Management System (CRUD + DB)

Overview
A console-based Python application to manage employee records using SQLite.  
This project demonstrates CRUD operations (Create, Read, Update, Delete) with proper data validation.

Features
- Add new employee records  
- View all employees  
- Update existing employee details  
- Delete employees  
- Data stored securely in SQLite database  

